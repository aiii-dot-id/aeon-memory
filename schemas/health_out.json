{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "health output",
  "type": "object",
  "properties": {
    "healthy": {
      "type": "boolean",
      "description": "True when nothing needed repair."
    },
    "stale_dropped": {
      "type": "integer",
      "description": "Index entries dropped: keys that were gone, and duplicates."
    },
    "zombie_dropped": {
      "type": "integer",
      "description": "Queue entries dropped: alive, missing, or duplicated."
    },
    "zombie_requeued": {
      "type": "integer",
      "description": "Evicted notes the queue had lost, queued again."
    },
    "occupancy": {
      "type": "integer",
      "description": "Notes holding key slots after the sweep."
    },
    "kv_scope": {
      "type": "string",
      "description": "The scope the key-value store reported for the last write."
    }
  },
  "required": [
    "healthy",
    "stale_dropped",
    "zombie_dropped",
    "zombie_requeued",
    "occupancy",
    "kv_scope"
  ]
}
