{
  "name": "search finds the superseding text, not the superseded",
  "operation": "search",
  "arguments": {
    "query": "volatile"
  },
  "expect": {
    "status": "succeeded",
    "result_contains": "\"match\":\"both\""
  }
}
